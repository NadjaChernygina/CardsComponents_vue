## 🚀 How to Run

1. **Clone or fork the repository** репозиторій
2. **Install dependencies:**
   ```bash
   pnpm install
   ```
3. **Start the development server:**
   ```bash
   pnpm dev
   ```

   src/
├── app/
├── featured/
│   └── auth/
|      └── components/
|         └── LoginForm.vue
├── pages/
│   └── Home/
|      └── components/
|      └── index.ts           # Main page
├── shared/
│   └── ui/
|      └── common/
│          └── VButton.vue
|          └── VCard.vue
|          └── VInput.vue
|          └── VLoader.vue
├── stores/
├── types/
├── widgets/

```